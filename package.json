{
  "name": "fillet",
  "version": "0.0.2",
  "description": "statistic functions for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/fillet"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/fillet/issues"
  },
  "homepage": "https://github.com/fishin/fillet",
  "engines": {
    "node": ">=0.12.x"
  },
  "scripts": {
    "test": "lab -a code -t 100 -v -m 2000 -L -r json -o lab.json -r html -o lab.html -r console -o stdout -r console -o console.log",
    "json": "lab -a code -t 100 -v -m 2000 -L"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "5.x.x"
  }
}
